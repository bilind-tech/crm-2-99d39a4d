{
  "name": "mycleancenter-backend",
  "version": "0.2.0",
  "private": true,
  "description": "MyCleanCenter Backend (Pi 5, Fastify + SQLite)",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node --enable-source-maps dist/server.js",
    "build": "tsc -p tsconfig.json && node -e \"const fs=require('fs'),path=require('path');const s=path.join('src','db','migrations'),d=path.join('dist','db','migrations');fs.mkdirSync(d,{recursive:true});for(const f of fs.readdirSync(s))if(f.endsWith('.sql'))fs.copyFileSync(path.join(s,f),path.join(d,f));console.log('Migrations copied:',fs.readdirSync(d).length);const fs2=path.join('src','pdf','fonts'),fd=path.join('dist','pdf','fonts');if(fs.existsSync(fs2)){fs.mkdirSync(fd,{recursive:true});for(const f of fs.readdirSync(fs2))fs.copyFileSync(path.join(fs2,f),path.join(fd,f));console.log('Fonts copied:',fs.readdirSync(fd).length);}\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@fastify/cookie": "^9.4.0",
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/multipart": "^8.3.0",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/static": "^7.0.4",
    "@node-rs/argon2": "^1.8.3",
    "better-sqlite3": "^11.3.0",
    "fastify": "^4.28.1",
    "googleapis": "^171.4.0",
    "imapflow": "^1.3.3",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.7",
    "pdfmake": "^0.2.10",
    "pino": "^9.4.0",
    "tar": "^7.5.13",
    "unzipper": "^0.12.3",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^20.14.10",
    "@types/node-cron": "^3.0.11",
    "@types/nodemailer": "^8.0.0",
    "@types/pdfmake": "^0.3.2",
    "@types/tar": "^7.0.87",
    "@types/unzipper": "^0.10.11",
    "jszip": "^3.10.1",
    "tsx": "4.20.6",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0"
  },
  "optionalDependencies": {
    "@node-rs/argon2-linux-arm64-gnu": "^1.8.3"
  }
}
